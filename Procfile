# Procfile
# This file tells Railway how to start the application

# For monorepo with separate backend and frontend services,
# Railway will detect and deploy based on Root Directory setting

# Backend service
# Configured via Railway Dashboard:
# - Root Directory: healthcare-backend
# - Build Command: npm install
# - Start Command: npm start

# Frontend service
# Configured via Railway Dashboard:
# - Root Directory: healthcare-frontend
# - Build Command: npm install && npm run build
# - Start Command: npm run preview

# Note: This Procfile is informational for Railway
# Railway will automatically detect Node.js projects
# and use package.json scripts as defined above
